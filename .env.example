# Claude Context Viewer Environment Configuration

# API Key Options (choose one):
# Option 1: Direct Anthropic API key
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxx

# Option 2: Use cliproxyapi (when no ANTHROPIC_API_KEY is set)
# The app will automatically detect cliproxyapi at ~/code/utilities/cliproxyapi
# You can customize the proxy location with these optional variables:
# CLIPROXYAPI_URL=http://localhost:8318       # Full URL to proxy (overrides path detection)
# CLIPROXYAPI_PATH=~/code/utilities/cliproxyapi  # Path to cliproxyapi installation
# CLIPROXYAPI_PORT=8318                        # Port for cliproxyapi (if not using URL)
# CLIPROXYAPI_KEY=your-proxy-api-key          # API key that matches cliproxyapi's auth config

# Optional: Server configuration
PORT=3001
NODE_ENV=development

# Optional: Claude model to use
CLAUDE_MODEL=claude-sonnet-4-20250514

# Optional: Enable extended thinking (for supported models)
ENABLE_THINKING=true
